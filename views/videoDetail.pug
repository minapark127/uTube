extends layouts/main

block content
    .video__player
        video(src=`/${video.fileUrl}`)
    .video__info
        a(href=routes.editVideo(video.id)) Edit Video
        h1.video__info-title=video.title
        span.video__info-views=video.views
        h2.video__info-description=video.description