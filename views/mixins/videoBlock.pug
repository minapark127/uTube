mixin videoBlock(video = {})
    .videoBlock
        video.videoBlock__thumbnail(src=video.videoFile, controls=true)
        h2.videoBlock__title
            a(href=routes.videoDetail(video.id))=video.title
        p.videoBlock__views=video.views 
