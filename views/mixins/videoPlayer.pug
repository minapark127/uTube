mixin videoPlayer(video = {})
    .videoPlayer#js-videoPlayer&attributes(attributes)
        video(src=`/${video.src}`)#js-video
        .videoPlayer__controls
            .videoPlayer__controls-column
                span#js-playBtn
                    i.fas.fa-play
                span#js-volumeBtn
                    i.fas.fa-volume-up
                span 00:00 / 00:00
            .videoPlayer__controls-column
                span#js-fullScreenBtn
                    i.fas.fa-expand
            
